# Methods

The prediction ladder. Each page derives its models term by term from a primary source, states where
the model fails, and records anything the source did not print.

Pages are added as each rung lands.
